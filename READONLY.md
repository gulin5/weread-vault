此本地版本按“已有笔记只打开，没有笔记才创建”的规则工作。

- 书架刷新、书籍详情刷新只读取数据。
- 点击书名或书籍上的笔记按钮：按书籍 ID 或书名查找已有笔记并打开；不存在时，在配置的笔记目录创建带有书籍属性和“读书笔记”标题的新文件。
- 新文件只创建一次；后续点击只打开，绝不重新生成正文。并发点击共用同一次创建；文件冲突时保留原文件并报错，不调用覆盖接口。
- 手动同步、强制同步、单书同步、定时同步均已禁用。
- 已有笔记和日记的改写、批量导入、删除、阅读统计保存、图片保存均已禁用。
- 缓存不再写入或清理仓库文件。插件自身的登录和界面设置仍可保存到 data.json。
- 按书籍 ID 或已有书名匹配笔记并打开的行为保留。

修改 main.js 后，需要在 Obsidian 的第三方插件设置中关闭再开启 Weread Vault，才能替换内存中的旧代码。

验证：`node --check main.js`，`node --test readonly.test.cjs`。

重新安装或更新插件会覆盖这份本地保护修改；更新后须重新检查，不能直接沿用不覆盖的保证。
